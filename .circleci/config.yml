version: 2
jobs:
  build:
    docker:
      - image: debian:stretch-slim
    steps:
      - checkout
      - run:
          name: install required apps
          command: |
            apt-get -yqq install wimtools git
  test:
    docker:
      - image: debian:stretch-slim
    steps:
      - checkout
      - run: echo 'hello'
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
